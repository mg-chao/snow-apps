[package]
name = "snow-recording-effects-c"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
snow-recording-effects = { path = "../snow-recording-effects" }
