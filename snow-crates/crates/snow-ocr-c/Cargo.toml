[package]
name = "snow-ocr-c"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
num_cpus = "1.16"
rapid-ocr-rs = { path = "../rapid-ocr-rs", default-features = false, features = ["static-onnx-runtime", "directml-provider"] }
