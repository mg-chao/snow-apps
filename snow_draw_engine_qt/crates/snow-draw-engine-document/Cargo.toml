[package]
name = "snow-draw-engine-document"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
snow-draw-engine-core = { path = "../snow-draw-engine-core" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"

[lib]
path = "src/lib.rs"
